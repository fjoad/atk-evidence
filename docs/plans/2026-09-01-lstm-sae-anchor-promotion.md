# Price and conditionally run one clean-reader LSTM-SAE anchor

**Date:** 2026-09-01

**User direction:** explain what would warrant a highly-implausible or
fabrication conclusion, then carry out the proposed prospective gate, costing,
and one watched LSTM-SAE anchor if it is affordable.

1. **Complete:** preserve the evidentiary boundary in
   `docs/decisions/2026-09-01-implausibility-and-fabrication-boundary.md`.
2. **Complete:** calculate the existing A16 projection from the preserved
   LSTM-SAE pilot and audited score recovery. Minimum ten-epoch projection is
   about 42.8 hours; the frozen 100-epoch ceiling is about 417.1 hours, so that
   device does not pass the existing 72-hour promotion gate.
3. **Complete:** freeze an adaptive operational decision-stability rule and
   one at-most-two-hour cost pilot on a single H200. It changes no scientific
   method field and exists only to determine whether an accessible faster GPU
   makes the full anchor affordable.
4. **In progress:** the cost-pilot implementation and wrapper are complete and
   pass all 256 repository tests (140 study and 116 root), strict data
   verification, Python compilation, shell syntax, and whitespace checks. The
   historical `run_experiment.py` and `remaining_models.py` hashes remain
   exact. Commit, push, and Panther synchronization remain.
5. **Pending:** run and audit exactly one H200 cost pilot.
6. **Conditional:** implement and launch one at-most-72-hour full
   `CR-ISET-LSTMSAE-01` anchor only if every operational gate passes, including
   a conservative 100-epoch projection no greater than 72 hours.
7. **Pending:** preserve the result or ineligibility outcome and stop for
   discussion. No FC-VAE, LSTM-VAE, AEA, mechanism wave, seed, Table-IV fit, or
   publication is included.

If the H200 projection fails, step 6 is skipped rather than converting a
partial fit into a full anchor.
